const io = require('../socket');


exports.connect = async (req, res, next) => {
  //console.log("GET /chat/  .connect");
  //
  //
  //   const {userId} = req;
  //
  //
  // io.getIO().emit('chat message', {msg: "wiadomosc"});
};

